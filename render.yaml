services:
  - type: web
    name: ameba-a42m
    env: node
    rootDir: .
    buildCommand: "npm ci && npm run build"
    startCommand: "node dist/server.js"
    healthCheckPath: "/healthz"          # 👈 NEW
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: SUPABASE_URL
        value: ""                         # set in dashboard
      - key: SUPABASE_ANON_KEY
        value: ""                         # set in dashboard
      - key: API_BASE
        value: ""                         # optional
