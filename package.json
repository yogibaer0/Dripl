{
  "name": "ameba",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },

  "scripts": {
"prepare:vendor": "node scripts/copy-supabase-umd.js",
    "build": "rimraf dist && tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts"
  },

  "dependencies": {
"@supabase/supabase-js": "^2.0.0",
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0"
  },

  "devDependencies": {
    "typescript": "^5.4.0",
    "rimraf": "^5.0.5",
    "ts-node-dev": "^2.0.0",
    "@types/node": "^20.11.30",
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "@types/morgan": "^1.9.7"
  }
}
